import { Player } from './player_class.js';

//create class for gamestatus
    //indicates whose turn it is - on click change player turn

    //check if someone has won or not after each turn
    //click New Game starts game
   
    

//create a method to indivate whose turn it is
    //depending on whose turn it is, token will change color

//create a funcion to add a token
    
// column class 
    //create method to check if columns are full
    // one array for a column 
    // with property tile 
    // 7 instances of column class
    //if full, add full class to HTML div



    let example = new Player('LeBron');
    console.log(example);